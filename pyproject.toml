[project]
name = "intership-project"
version = "0.1.0"
description = "AI-based resume screening and recruitment system using embeddings, Pinecone and a Groq-hosted LLM"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
